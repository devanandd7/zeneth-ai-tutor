# Kokoro TTS API - Requirements
# Install with: pip install -r requirements.txt

# Core TTS
kokoro>=0.9.4
soundfile>=0.12.1

# API server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Audio processing
numpy>=1.24.0

# Optional: for MP3 export support (requires ffmpeg installed on OS)
# pydub>=0.25.1